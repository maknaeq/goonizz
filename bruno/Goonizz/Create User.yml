info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}users"
  auth: inherit
  body: json

body:
  json: |-
    {
      "email": "user@example.com",
      "password": "password123",
      "role": "user"
    }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5