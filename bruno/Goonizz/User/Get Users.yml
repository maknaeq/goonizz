info:
  name: Get Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}users"
  auth: inherit

runtime:
  variables:
    - name: BASE_URL
      value: 127.0.0.1:3300/

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
